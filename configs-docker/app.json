{
    "environment" : "local",        // Environment. ["local", "test", "prod"]
    "docker" : true,                // Docker flag. [false, true]
    "template-cache-size" : 10000   // Cache size of templates. [0-infinity]
}